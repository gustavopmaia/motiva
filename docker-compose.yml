services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: ./apps/backend/.env
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped

  generator:
    build:
      context: .
      dockerfile: Dockerfile
      target: deps
    volumes:
      - ./apps/backend/drizzle:/app/apps/backend/drizzle
    environment:
      DATABASE_URL: postgresql://x:x@localhost:5432/x
    command: node_modules/.bin/drizzle-kit generate --config apps/backend/drizzle.config.ts
    profiles: [generate]

  migrator:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: ./apps/backend/.env
    command: node apps/backend/dist/src/migrate.js
    profiles: [migrate]
